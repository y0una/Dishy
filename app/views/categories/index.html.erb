<!-- HOME PAGE  -->
<head>
</head>
<body id="home">
  <div class="white-bg-home">
    <div class="home-links">
      <% @categories.each do |category| %>
        <h2><%= link_to category.name, category_dishes_path(category) %></h2>
      <% end %>
    </div>
  </div>
</body>