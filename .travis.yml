services:
- postgresql
before_script:
- psql -c 'create database dishy_test;' -U postgres
language: ruby
script:
- export RAILS_ENV=test
- bundle exec rake db:create db:schema:load db:test:prepare
- bundle exec rspec --color --format documentation --require spec_helper --require rails_helpe
rvm:
- 2.3.1
